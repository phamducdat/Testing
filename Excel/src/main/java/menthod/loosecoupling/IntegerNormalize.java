package menthod.loosecoupling;

import org.apache.poi.ss.usermodel.Cell;

public class IntegerNormalize implements Normalizer{
    @Override
    public Comparable normalize(Cell cell) {
        return null;
    }
}
